__pycache__/
*.py[cod]
*$py.class
*.egg-info
.pytest_cache
.ipynb_checkpoints

thumbs.db
.DS_Store
.idea

# Whisper cache and weight directories. Checkpoints stay out of git.
# load_model() writes ~/.cache/whisper or $XDG_CACHE_HOME/whisper
# (whisper/__init__.py). --model_dir / download_root can point inside
# this checkout; a local weights/ dump is ignored the same way.
.cache/
cache/
weights/
*.pt
*.pth

